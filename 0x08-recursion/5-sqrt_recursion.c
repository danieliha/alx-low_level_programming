#include "main.h"
#include  <stdlib.h>
#include <stdio.h>

/**
 * number - help find the square root of natural squareroot numbers.
 * 2n: natural number starting from 1
 * @root: squareroot number to be found.
 * 
 * Return the root of the answer.
 * natural square root return -1
 * if root less than 0 return -1
 */
int find_sqrt(int num,int root)
int _sqrt_recursion(int n)
{
	if (n < 0)
		return (-1);
	return(number(1, n));
}
